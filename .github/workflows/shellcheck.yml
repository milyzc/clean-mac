name: Lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  zsh-syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install zsh
        run: sudo apt-get install -y zsh
      - name: Check syntax
        run: zsh -n clean.sh
